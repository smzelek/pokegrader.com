import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stevezelek.pokegrader',
  appName: 'pokegrader.js',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
